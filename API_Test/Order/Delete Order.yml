info:
  name: Delete Order
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/orders/PASTE_ORDER_UUID"
  auth:
    type: bearer
    bearer:
      token: "{{access_token}}"

runtime:
  variables:
    - name: base_url
      value: http://localhost:8888/api/v1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
