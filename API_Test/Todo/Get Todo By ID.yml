info:
  name: Get Todo By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/todos/{{todo_id}}"
  auth:
    type: bearer
    bearer:
      token: "{{access_token}}"

runtime:
  variables:
    - name: base_url
      value: http://localhost:8888/api/v1
    - name: todo_id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
