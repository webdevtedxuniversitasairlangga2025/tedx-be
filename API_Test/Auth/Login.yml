info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "muhamadrizalnurohman072005@gmail.com",
        "password": "password123"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8888/api/v1
    - name: email
      value: pelajarbintangrumy@gmail.com
    - name: password
      value: password123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
