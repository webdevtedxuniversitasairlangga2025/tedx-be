info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "ernest",
        "email": "ernestnaufal22@gmail.com",
        "password": "passwordernest"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8888/api/v1
    - name: email
      value: pelajarbintangrumy@gmail.com
    - name: password
      value: password123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
