info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Rizal Admin",
        "email": "mohamad.rizal.nurochman-2024@fst.unair.ac.id",
        "password": "password123",
        "role":"admin"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: https://tedxuniversitasairlangga.com
    - name: email
      value: pelajarbintangrumy@gmail.com
    - name: password
      value: password123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
